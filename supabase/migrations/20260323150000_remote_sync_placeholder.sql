-- No-op placeholder: this version exists on the linked remote database but was missing from the repo.
-- Aligns local migration filenames with remote supabase_migrations.schema_migrations.
SELECT 1;
