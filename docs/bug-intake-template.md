# Bug Intake Template (Internal)

## الصفحة
- المسار (URL) / اسم الشاشة:
- الدور الذي اختبر:

## الدور
- `role` (owner/master_admin/branch_manager/cashier/pharmacist/warehouse/procurement/kitchen/doctor/receptionist):

## الفرع
- `branch` المستخدم (إن كان ينطبق):
- ما الفرع الذي كان متوقعًا أن تُستخدم بياناته:

## الـ Tenant
- `tenant_id` (إن أمكن تحديده):

## الخطوات (Steps to Reproduce)
1.
2.
3.

## المتوقع (Expected)
- ما الذي كان يجب أن يحدث (بدون empty/بدون error خام):

## الذي حدث بالفعل (Actual)
- ما الذي حدث فعليًا:
- هل ظهر empty state؟ هل كان واضح السبب؟
- هل ظهر error عربي أم raw technical؟

## بيانات مساعدة (Attachments)
- Screenshot إن وجد:
- Network error إن وجد (انسخ رسالة/كود response إن أمكن):
- أي Console error (إن وجد):

## التصنيف المقترح
- Permissions / Role redirect
- Tenant mismatch
- Branch mismatch
- RLS denial
- UI stale / refresh missing
- Data sparsity / seed missing
- Other

